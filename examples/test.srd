fn exit code
	rbx = code
	syscall 60
end

fn minm x1 x2 :result
	if x1 < x2
		result = 0
	else
		result = 1
	end
end

fn mult n times :result
	minm n times :nth

	if nth == 1
		times <=> n
	end

	result = n

	while times > 1
		result += n
		times -= 1
	end
end

fn main
	mult 6 10
	exit rax
end
